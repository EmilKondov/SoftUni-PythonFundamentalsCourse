import math
secret_message = input().split()

ascii_table_code = []

for word in secret_message:
    for character in word:
        if 
            ascii_table_code.append(int(character))





print(secret_message)